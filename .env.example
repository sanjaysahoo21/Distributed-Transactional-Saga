# ============================================================
# Database Configuration
# ============================================================
DB_NAME=saga_db
DB_USER=saga_user
DB_PASSWORD=saga_pass

# ============================================================
# Inter-Service Communication URLs (Docker internal network)
# ============================================================
PAYMENT_SERVICE_URL=http://payment-service:8081
INVENTORY_SERVICE_URL=http://inventory-service:8082

# ============================================================
# Spring Framework Environment Variables
# (These are set automatically in docker-compose.yml using
#  the values above. Listed here for documentation purposes.)
# ============================================================
# SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/saga_db
# SPRING_DATASOURCE_USERNAME=saga_user
# SPRING_DATASOURCE_PASSWORD=saga_pass
# SERVER_PORT=8080  (order-service)
# SERVER_PORT=8081  (payment-service)
# SERVER_PORT=8082  (inventory-service)
